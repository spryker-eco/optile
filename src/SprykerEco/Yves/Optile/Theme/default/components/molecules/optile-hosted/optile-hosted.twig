{% extends model('component') %}

{% define config = {
    name: 'optile-hosted',
    tag: 'optile-hosted',
} %}

{% define attributes = {
    id: config.name,
    'data-long-id': required,
    'data-base-url': required,
    'data-submit-id': required,
    'data-submit-container-id': required,
} %}
