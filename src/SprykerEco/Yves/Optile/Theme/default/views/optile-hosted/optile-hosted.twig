{% extends model('template') %}

{% define data = {
    longId: longId,
    baseUrl: 'https://api.sandbox.oscato.com/pci/v1/',
    submitId: 'payment-submit-button',
} %}

{% block body %}
    {% include molecule('optile-hosted', 'Optile') with {
        data: {
            longId: data.longId,
            baseUrl: data.baseUrl,
            submitId: data.submitId,
        },
    } only %}
{% endblock %}
